# 分任务开发清单

这份清单用于组内分工。

更新时间：2026-06-06

当前状态：本机 demo、Neo4j 留痕、默认输出数据和桌面交付文件夹均已准备好。后续小组成员主要做三类工作：改展示数据、接可选 API、准备 PPT/截图。

桌面交付路径：

```text
C:\Users\cheng\Desktop\CampusMatchAI_小组作业_最新
```

统一验收命令：

```bash
python scripts/run_pipeline.py --n-users 30 --top-k 5
python3 -m compileall src scripts app.py
streamlit run app.py
```

## A. 合成问卷与用户画像

负责文件：

```text
src/campus_match/synthetic_users.py
src/campus_match/profile_extraction.py
```

交付：

1. 100-300 个合成用户。
2. 结构化 `profiles.jsonl`。
3. 用户自我介绍和理想型文本。

验收：

```bash
python scripts/run_pipeline.py --n-users 30 --top-k 3
```

## B. 图片生成与多模态

负责文件：

```text
src/campus_match/image_generation.py
src/campus_match/embeddings.py
```

交付：

1. 每个用户一张合成头像或生活方式图。
2. 每个用户一份图片 prompt。
3. 图片 embedding。

验收：

```text
images/U001.png
indexes/image_embeddings.npy
```

## C. 知识图谱

负责文件：

```text
src/campus_match/kg.py
src/campus_match/neo4j_trace.py
scripts/import_neo4j.py
```

交付：

1. `triples.csv`。
2. `knowledge_graph.gexf`。
3. GraphRAG 路径证据。
4. Neo4j 留痕文件：节点 CSV、关系 CSV、导入 Cypher、查询 Cypher。
5. 可选：本机 Neo4j 导入截图。

验收：

```text
data/triples.csv
outputs/knowledge_graph.gexf
outputs/neo4j/campus_match_ai_nodes.csv
outputs/neo4j/campus_match_ai_relationships.csv
outputs/neo4j/import_campus_match_ai.cypher
outputs/neo4j/demo_queries.cypher
```

如果本机安装 Neo4j，可选验收：

```bash
python scripts/import_neo4j.py --uri bolt://localhost:7687 --user neo4j --password 你的密码 --clear
```

课程对应：

1. 第 3 章：知识图谱实体、关系、三元组。
2. 第 5 章：Neo4j 图数据库、Cypher 查询、图谱可视化。
3. 第 10 章：GraphRAG 多跳路径证据。

## D. 匹配算法

负责文件：

```text
src/campus_match/matching.py
```

交付：

1. Top-K 推荐结果。
2. 匹配分数分解。
3. 冲突惩罚。

验收：

```text
outputs/matches.jsonl
```

## E. GraphRAG 推荐解释

负责文件：

```text
src/campus_match/graph_rag.py
```

交付：

1. 推荐理由。
2. 共同点。
3. 差异点。
4. 风险提示。
5. 破冰话题。

验收：

```text
outputs/matches_with_explanations.jsonl
```

## F. A100 加分项：GNN 链接预测

负责文件：

```text
src/campus_match/gnn.py
```

交付：

1. 构建伪标签。
2. 训练 GraphSAGE/GCN。
3. 输出 link score。
4. 与规则匹配做对比。

当前代码已有 GraphSAGE 链接预测实现。如果环境没有 `torch` / `torch-geometric`，会自动降级并输出说明。A100 环境安装完整依赖后可直接运行：

```bash
python scripts/run_pipeline.py --n-users 200 --top-k 5 --run-gnn
```

## G. 前端 Demo

负责文件：

```text
app.py
```

交付：

1. 用户画像页。
2. 推荐列表页。
3. GraphRAG 解释页。
4. 图片展示。
5. 图谱证据展示。
6. Neo4j 留痕展示。
7. 动态场景、恋爱热度、线下约会和知识治理展示。

验收：

```bash
streamlit run app.py
```

## H. 动态场景任务匹配

负责文件：

```text
src/campus_match/dynamic_scene.py
app.py
```

交付：

1. 模拟实时任务和未来预约任务。
2. 任务意图抽取，如饭搭子、学习搭子、运动搭子、Citywalk。
3. 时间窗口、地点、任务类型、语义相似度和安全上下文的动态排序。
4. Streamlit 页面展示“实时流文本、动态因子、安全上下文、候选搭子排序”。

验收：

```text
outputs/scene_requests.jsonl
outputs/scene_matches.jsonl
```

课程对应：

1. 第 2 章：知识管理动态性与应用闭环。
2. 第 3 章：地点、任务、时间窗口知识建模。
3. 第 5 章：图数据库/图查询可扩展到地点图谱。
4. 第 7 章：任务文本语义检索。
5. 第 9 章：场景意图抽取。
6. 第 10 章：安全上下文 RAG 提醒。

## I. 恋爱动态与线下约会上下文

负责文件：

```text
src/campus_match/relationship_dynamics.py
src/campus_match/date_context.py
app.py
```

交付：

1. 基于合成聊天聚合指标生成恋爱热度曲线。
2. 根据热度变化生成个人画像更新信号。
3. 基于模拟 LBS、天气、地点安全等级生成线下约会建议。
4. Streamlit 页面展示热度曲线、画像更新、天气/位置/安全上下文。

验收：

```text
outputs/relationship_dynamics.jsonl
outputs/date_contexts.jsonl
```

课程对应：

1. 第 2 章：知识管理动态性、生命周期和应用闭环。
2. 第 3 章：地点、天气、互动事件知识建模。
3. 第 5 章：地点图谱和路径查询可扩展。
4. 第 9 章：互动摘要和画像更新 Prompt 可扩展。
5. 第 10 章：基于位置/天气/安全上下文的 RAG 建议。

## J. 知识治理与惩罚项

负责文件：

```text
src/campus_match/governance.py
app.py
```

交付：

1. 合成失约、迟取消、不适反馈、骚扰标记、正反馈事件。
2. 信用分计算。
3. 推荐可见度下降。
4. 闪电搭子发布冷却。
5. 条件禁言和人工复核标记。

验收：

```text
outputs/governance_records.jsonl
matches.jsonl 中 candidate_governance / governance_penalty 字段
scene_matches.jsonl 中 candidate_governance / governance_penalty 字段
```

课程对应：

1. 第 2 章：知识治理、安全政策、动态应用闭环。
2. 第 4 章：动态图权重和节点可见度调整。
3. 第 5 章：用户-事件-策略图谱可扩展。
4. 第 10 章：把治理策略作为 RAG 风险提示上下文。

## K. 汇报材料与截图

负责内容：

```text
项目最新框架与待办.md
README.md
docs/本机准备清单.md
pdf版本课件/
```

交付：

1. PPT 项目总览页。
2. 方法链路图：合成数据 -> 知识图谱 -> 向量检索 -> 图计算 -> GraphRAG -> 动态治理。
3. 课程章节映射表。
4. Streamlit demo 截图。
5. Neo4j Browser 或前端 Neo4j 留痕截图。
6. 伦理与隐私说明。

验收：

```text
截图包含：画像、推荐、GraphRAG 路径、Neo4j 留痕、闪电搭子、热度曲线、线下约会、治理策略
```

课程对应：

1. 第 1 章：AI4KM / KM4AI 总定位。
2. 第 2 章：知识管理闭环与知识治理。
3. 第 3-5 章：知识图谱、图计算、图数据库。
4. 第 6-10 章：多模态、向量库、LLM、Prompt、RAG。
