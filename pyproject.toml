[project]
name = "campus-match-ai"
version = "0.1.0"
description = "Knowledge-enhanced synthetic campus matching demo"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "pillow>=10.0",
  "requests>=2.31"
]

[tool.setuptools.packages.find]
where = ["src"]
